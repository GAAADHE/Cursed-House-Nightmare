extends Node2D

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.
func _process(delta):
	"""
	if get_node("up").is_pressed():
		Input.action_press("player_up")
	else:
		Input.action_release("player_up")
	
	if get_node("down").is_pressed():
		Input.action_press("player_down")
	else:
		Input.action_release("player_down")
	if get_node("left").is_pressed():
		Input.action_press("player_left")
	else:
		Input.action_release("player_left")
	if get_node("right").is_pressed():
		Input.action_press("player_right")
	else:
		Input.action_release("player_right")
	if get_node("bomb").is_pressed():
		Input.action_press("player_atack")
	else:
		Input.action_release("player_atack")"""
# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
